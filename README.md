# Midterm Q1

A simple counter that increments the number with every press of the increment button. The increment and reset button uses a lambda and increments/resets a state variable. The state variable uses rememberSaveable to survive screen rotation.

# AI Disclosure

No AI was used for this problem.
